# AutoResearch Toy Speed Fixture

This fixture verifies a Codex-native AutoResearch loop without touching real project data.

The fixture has an overall baseline/candidate pair and train/holdout split files. The holdout split drives the keep/discard decision when it exists.
